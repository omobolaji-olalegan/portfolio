services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: cyber-portfolio:latest
    container_name: cyber-portfolio
    ports:
      - "8080:80"
    restart: unless-stopped
